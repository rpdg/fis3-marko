interface FisUtil {
	exists(filepath: string): boolean;
	readJSON(path: string): any;
}
interface FisStatic {
	project: any;
	log: any;
	config: any;
	on: (str: string, cb: Function) => any;
	util: FisUtil;
}

declare let fis: FisStatic;

declare type FisFile = {
	fullname: string;
	id: string;
	isHtmlLike: boolean;
	realpathNoExt: string;
	subpath: string;
	cache: any;
	release: string | boolean;
};

declare type KeyValueObject = { [k: string]: any };

declare type MarkoOption = {};
